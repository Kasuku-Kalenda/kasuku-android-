# Dépendances Node
node_modules/

# Build Vite (généré par npm run sync — ne pas committer)
# Les assets web sont copiés dans android/app/src/main/assets/public/ par cap sync

# Android build
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/.idea/
android/**/*.iml
android/captures/

# Keystore (ne jamais committer les clés de signature)
*.jks
*.keystore

# Google Services (contient des clés API)
android/app/google-services.json
