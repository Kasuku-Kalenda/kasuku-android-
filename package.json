{
  "name": "kasuku-android",
  "version": "1.0.0",
  "description": "Kasuku — App Android (Capacitor wrapper)",
  "scripts": {
    "sync": "cd ../kasuku-cultural-calendar-170426 && VITE_API_URL=https://kasuku.afrikia.org/api/v1 npm run build && cd ../kasuku-android && npx cap sync android",
    "sync:dev": "CAPACITOR_DEV=true npx cap sync android",
    "sync:prod": "cd ../kasuku-cultural-calendar-170426 && VITE_API_URL=https://kasuku.afrikia.org/api/v1 npm run build && cd ../kasuku-android && npx cap sync android",
    "open": "npx cap open android",
    "run": "npx cap run android",
    "android": "npm run sync:prod && npm run open",
    "android:dev": "npm run sync:dev && npm run open"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.3.3",
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3",
    "@capacitor/device": "^8.0.2",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/push-notifications": "^8.1.1",
    "@capacitor/splash-screen": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "typescript": "^6.0.3"
  }
}
