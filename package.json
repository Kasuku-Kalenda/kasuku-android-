{
  "name": "kasuku-android",
  "version": "1.0.0",
  "description": "Kasuku — App Android (Capacitor wrapper)",
  "scripts": {
    "sync": "cd ../kasuku-cultural-calendar-170426 && VITE_API_URL=https://kasuku.afrikia.org/api/v1 npm run build && cd ../kasuku-android && npx cap sync android",
    "open": "npx cap open android",
    "run": "npx cap run android",
    "android": "npm run sync && npm run open"
  },
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.3.3",
    "@capacitor/cli": "^8.3.3",
    "@capacitor/core": "^8.3.3",
    "@capacitor/status-bar": "^8.0.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
